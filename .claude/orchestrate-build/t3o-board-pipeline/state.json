{
  "workstream": "t3o-board-pipeline",
  "integration_branch": "feature/t3o-board-pipeline",
  "base": "t3o",
  "spec_update": "Mid-build spec change applied. Merged local t3o (2 spec commits 711393673 + 7e0913603) into integration branch (-> 07442764f, pushed) and into feature/t3o-15-stage-pipeline (-> 54ed592b2, pushed; plan-doc add/add conflict resolved by taking t3o's new spec; grep D15 == 4 confirms). Note: origin/t3o itself NOT pushed (spec commits are local-only on t3o); they rode into feature branches via merge.",
  "plans": {
    "t3o-13-archived-card-dependencies": {
      "status": "merged",
      "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/21"
    },
    "t3o-15-stage-pipeline": {
      "status": "merged",
      "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/23",
      "squash_commit": "7bc9da835",
      "rounds_to_converge": 3,
      "note": "D15 seam built (stageExecutor.ts: pure planNext, SimpleStageExecutor, stageExecutorForRole registry consulted only in beginStageRun). BoardStageExecution is {kind:'simple'} discriminated union -> t3o-16 widens to Union(simple,review). Fixed pre-existing migration-014 doubled-column bug; adapted 13 stale tests (0 prod bugs). Next free board migration = 015. Kept D5/D6 role/mode policy branches per corrected criterion 20.",
      "branch": "feature/t3o-15-stage-pipeline",
      "worktree": ".claude/worktrees/agent-a06928787d948deda",
      "current_agent": "a97d436fe43a0a503",
      "pushed_to_origin": true,
      "note": "Only OUTSTANDING work = D15 seam (stageExecutor.ts does not exist yet; everything else built, migration 014_BoardStages.ts present). Corrected spec commit e2913c275 merged into branch (tip ea6c7c094) AND integration (185aa5165). CRITERION 20 CORRECTED + NARROWED: only EXECUTION-SHAPE 'what to execute' branching moves behind the registry. The stage.role==='build' D6 checks (per-card human-in-the-loop) and D5 Mode-driven worktree/slot decisions are CORRECT POLICY and MUST STAY. Prior over-strict agent (ad92) was stopped during exploration before it stripped anything. New agent a97d builds stageExecutor.ts (interface + SimpleStageExecutor wrapping the inline 'what runs next' logic + role registry), keeps D5/D6, confirms {kind:'simple'} union, adds planNext unit test (crit 19)."
    },
    "t3o-14-card-thread-menu": {
      "status": "merged",
      "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/24",
      "squash_commit": "2b58ec066",
      "merge_note": "CAPPED@round=5, 0 crit/0 impr/0 nit, 0 deadlocks -> merged per policy. UI-only; salvaged UI half of planning-thread-autospawn, discarded boardCardThreadSpawn.ts + planning gate; restart re-implemented via generic board.card.start-stage-thread; ZERO server/contracts changes. Landed first, no rebase needed. gh --delete-branch errored locally (base checked out in sibling worktree) but squash merge succeeded; remote branch deleted manually.",
      "prerequisites": ["t3o-06", "t3o-15"],
      "note": "REWRITTEN: now UI-only (card thread pane '+' dropdown). Old t3o-14-planning-thread-autospawn is superseded (planning-autospawn subsumed by t3o-15 generic auto-kickoff). Salvage ONLY the UI half of unmerged branch feature/planning-thread-autospawn (df78df864): BoardCardThreadAddMenu.tsx, BoardCardThreadPane.tsx, boardCardThreadSpawn.ts, BoardCardDetail wiring. DISCARD its supervisorReactor.ts planning-spawn path and contracts additions (t3o-15 replaces them). Do NOT merge that branch as-is."
    },
    "t3o-16-review-loop": {
      "status": "merged",
      "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/26",
      "squash_commit": "a15219a17",
      "merge_note": "Converged round 3, 0 deadlocks. NOTABLE DEVIATION: modified the supervisor reactor (which the spec forbade) via the spec's own escape hatch ('if the seam needs it, the seam is the bug') — the shipped t3o-15 seam called planNext only once at stage entry, so a multi-phase review loop couldn't run. Added a GENERIC, role-blind 'ask executor what runs next on a succeeded step' continuation (no if stage.role==='review' anywhere). Fresh-eyes loop reviewed it, did not flag. Per-phase slot release/reacquire (AC12 permits). New file reviewLoopExecutor.ts. FLAG FOR OWNER REVIEW.",
      "prerequisites": ["t3o-15"],
      "note": "NOW IN SCOPE (previously excluded). Spec is complete/buildable. Runs in PARALLEL with t3o-14/t3o-17 once t3o-15 merges. MUST NOT modify supervisor reactor, decider, projector, or MCP toolkit — if it needs to, the D15 seam was built wrong; fix the seam, not t3o-16. Adds {kind:'review',...} to the BoardStageExecution union."
    },
    "t3o-17-stall-detection": {
      "status": "merged",
      "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/25",
      "squash_commit": "817710642",
      "merge_note": "Converged round 2, 0 deadlocks. Migration 015_BoardCardStepStall. recoveryDecision stays pure; maxInvocationsPerStageEntry on stage config (no extra column). Criterion 13 implemented generically + unit-tested; t3o-16 not in tree so not e2e-tested against a real review loop. Applied cleanly, no rebase (disjoint from t3o-14 UI-only diff). gh --delete-branch local switch failed (base checked out in sibling worktree) but server-side squash merged; remote branch deleted manually.",
      "prerequisites": ["t3o-15"],
      "plan_added_by_commit": "4afe56603",
      "note": "ADDED mid-build (integration tip 427e6ee13 carries the plan). Splits recovery counter into attempt (cumulative) vs stallCount (consecutive, reset by board_report_progress or a new commit); adds distinct 'stalled' status (vs awaiting-input); releases concurrency slot on give-up; adds per-stage-entry invocation ceiling (maxInvocationsPerStageEntry, default 20). Touches contracts/board.ts, supervisor.ts (recoveryDecision stays PURE — takes progressedSinceLastNudge bool), supervisorReactor.ts, decider.ts/projector.ts, a NEW migration (next free integer — 014_BoardStages exists, so 015+), and board UI. INTERACTION: modifies the reactor/decider/projector that t3o-16 must NOT touch, so those two don't collide there; shares contracts/board.ts additions with t3o-16 (rebase-retry handles it). Criterion 13 ('with t3o-16 landed...') is a runtime cross-check, not a build prereq — declared prereq is t3o-15 only. Runs PARALLEL with t3o-14 and t3o-16 after t3o-15 merges."
    }
  },
  "launch_after_t3o15_merges": [
    "t3o-14-card-thread-menu",
    "t3o-16-review-loop",
    "t3o-17-stall-detection"
  ],
  "build_complete": true,
  "final_integration_tip": "3b686259a",
  "final_typecheck": "clean (orchestrator independently re-ran vp run -r typecheck: exit 0, 15/15 packages, no error TS)",
  "next_step": "Merge feature/t3o-board-pipeline -> t3o when satisfied (orchestrator does NOT do the base merge).",
  "integration_fix": {
    "id": "fix-stage-exec-ceiling",
    "status": "merged",
    "pr_url": "https://github.com/brentkelly/t3code-orchestrator/pull/27",
    "squash_commit": "3b686259a",
    "current_agent": "ad220860a3e204a51",
    "branch": "fix/t3o-stage-exec-ceiling",
    "reason": "Post-merge integration sanity check (orchestrator ran recursive typecheck on the combined tree) caught TS2339 at supervisorReactor.ts:906 — the review union variant (t3o-16) lacked maxInvocationsPerStageEntry that the reactor (t3o-17) reads off the union. GitHub auto-merged the two parallel contracts/board.ts edits textually clean but semantically broken; no single plan agent could see it. Fix: add the field to BoardStageExecutionReview (mirrors simple variant; also satisfies t3o-17 criterion 13). Agent verifies full recursive typecheck + board suites green, then PRs+merges (fast review loop, proportionate for a one-field alignment).",
    "note_for_report": "This validates the integration-check step: parallel plans that both edit the same contracts file can auto-merge into a non-compiling tree; always typecheck the combined branch before recommending the base merge."
  },
  "constraints": {
    "review_depth": "default, 5 rounds; no --fast/--focused",
    "migrations": "t3o_sql_migrations ledger, next free integer from 014, register in apps/server/src/board/migrations/index.ts",
    "checkmark_rename": "commit with --no-verify (formatter hook fails on .plans/)",
    "t3o15_upstream_edit": "only: add 'stage' to OrchestrationAggregateKind in packages/contracts/src/orchestration.ts:1088"
  }
}
